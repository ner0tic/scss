{% from "macros/fields.html" import render_text_field, render_select_field, render_form_field, render_form_fields %}
{% extends "layout.jinja2" %}

{% block content %}
  <div class="container">
    {% if msg != None %}<p>{{msg}}</p>{% endif %}
    <form name="add_facility" method="post" novalidate class="form">
        <div class="form__container">
          {{ render_form_fields(form) }}
          {# {{ render_text_field(form.name) }} #}
          {#  {{ render_text_field(form.description) }} #}
          {#  {{ render_select_field(form.organization_id) }} #}
          {#  {{ render_form_fields(form.address_id) }}  , form.address_id.label) }} #}
            {{ form.hidden_tag() }}
        </div>
      </form>
  </div>
{% endblock %}