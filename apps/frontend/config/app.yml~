# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/11-App

# default values
all:
  max_items_on_index: 10
  min_periods_per_week: 3
  tte_periods_per_week: 7
  patrol_upload_logo_dir: /images/user_files/logos/patrol
  autofill_class_search_max: 20
  menu_file: menu.yml
  melody:
    create_user: true            # create a new user if not logged when you connect to a service
    facebook:
      key: 173494226064106
      secrect: 41fbd98aa2efaeb12b2602149710ca86
      callback: @fb_connect
      scope: [email]             #optional

      user:
        username:
          call: me
          path: id
          prefix: Facebook_
        first_name:
          call: me
          path: first_name
        last_name:
          call: me
          path: last_name
        email_address:
          call: me
          path: email
          key: true
      twitter:
        key: xxxxxxxxxxxxx
        callback: @mymodule_twitter
        scope: [email]

      Google:
        key: xxxxxxxxxxxxxxxx
        callback: @mymodule_twitter
        #needed for google or facebook
        api: [contacts, ...]        # only for google - easy way to set scopes
        scope: [permission_1, ...]  # for google and facebook to set permissions - for google prefere api parameter
