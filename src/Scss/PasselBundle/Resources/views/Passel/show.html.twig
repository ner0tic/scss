{% extends '::base.html.twig' %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('/bundles/scssuser/css/Base/show.css') }}" rel="stylesheet" />
    <link href="{{ asset('/bundles/scsspassel/css/passel.css') }}" rel="stylesheet" />    
{% endblock %}

{% block main_content %}

<table class="table">
    <tbody>
        <tr>
            <th>{{ 'name' | title }}</th>
            <td>{{ passel.name }}</td>
        </tr>
        <tr>
            <th>{{ 'region' | title }}</th>
            <td>{{ passel.region }}</td>
        </tr>
        <tr>
            <th>{{ 'council' | title }}</th>
            <td>{{ passel.council }}</td>
        </tr>
        <tr>
            <th>{{ 'organization' | title }}</th>
            <td>{{ passel.council.organization }}</td>
        </tr>        
    </tbody>    
</table>
{% endblock %}