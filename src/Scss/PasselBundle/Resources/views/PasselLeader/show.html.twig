{% extends '::base.html.twig' %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('/bundles/scssuser/css/Base/show.css') }}" rel="stylesheet" />
    <link href="{{ asset('/bundles/scsspassel/css/passel_leader.css') }}" rel="stylesheet" />    
{% endblock %}

{% block main_content %}

<table class="table">
    <tbody>
        <tr>
            <th>{{ 'last name' | title }}</th>
            <td>{{ passel_leader.last_name }}</td>
        </tr>
        <tr>
            <th>{{ 'first name' | title }}</th>
            <td>{{ passel_leader.first_name }}</td>
        </tr>
        <tr>
            <th>{{ 'dob' | title }}</th>
            <td>{{ passel_leader.dob }}</td>
        </tr>
        <tr>
            <th>{{ 'passel' | title }}</th>
            <td>{{ passel_leader.passel }}</td>
        </tr>
        <tr>
            <th>{{ 'region' | title }}</th>
            <td>{{ passel_leader.region }}</td>
        </tr>
        <tr>
            <th>{{ 'council' | title }}</th>
            <td>{{ passel_leader.council }}</td>
        </tr>
        <tr>
            <th>{{ 'organization' | title }}</th>
            <td>{{ passel_leader.passel.council.organization }}</td>
        </tr>        
    </tbody>    
</table>
{% endblock %}